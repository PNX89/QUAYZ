apiVersion: v2
name: deploy-canary
description: A workload whose liveness and readiness probes disagree on purpose, so the difference between the two can be observed rather than described
type: application
version: 0.1.0
appVersion: "0.1.0"
