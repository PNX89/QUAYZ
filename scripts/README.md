Scripts that produce the committed evidence in this repository.

Each one is the single path for the artefact it writes: CI runs the same script and fails if the
committed output has drifted, so no file here is a claim about the past that nothing regenerates.
